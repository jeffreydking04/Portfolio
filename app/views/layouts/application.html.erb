<!DOCTYPE html>
<html>
<head>
  <title>jd king, software engineer</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
    <div class="container">
      <ul class="nav nav-tabs">
        <li><%= link_to "Home", root_path %></li>
        <li><%= link_to "Resume", resume_path %></li>
        <li><%= link_to "Blog", posts_path %></li>
        <li><%= link_to "Projects", projects_path %></li>
        <li><%= link_to "About", about_path %></li>
        <% if (current_user && current_user.name == 'jeffrey') %>
          <li><%= link_to "Create User", new_user_path %></li>
        <% end %>
        <div class="pull-right user-info">
          <% if current_user %>
            <div class="pull-right">
              <%= link_to "Sign Out", session_path(current_user), method: :delete, class: 'btn btn-dutch' %>
            </div>
            <div class="pull-right name">
              <%= current_user.name %>
            </div>
          <% else %>
            <%= link_to "Sign In", new_session_path, class: 'btn btn-dutch' %>
          <% end %>
        </div>
      </ul>
      <br><br>
      <div class="container">
        <%= yield %>
      </div>
    </div>
</body>
</html>
